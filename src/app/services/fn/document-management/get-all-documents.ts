/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Pageable } from '../../models/pageable';
import { PageDocumentShortDto } from '../../models/page-document-short-dto';

export interface GetAllDocuments$Params {
  pageable: Pageable;
}

export function getAllDocuments(http: HttpClient, rootUrl: string, params: GetAllDocuments$Params, context?: HttpContext): Observable<StrictHttpResponse<PageDocumentShortDto>> {
  const rb = new RequestBuilder(rootUrl, getAllDocuments.PATH, 'get');
  if (params) {
    rb.query('pageable', params.pageable, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageDocumentShortDto>;
    })
  );
}

getAllDocuments.PATH = '/documents';
