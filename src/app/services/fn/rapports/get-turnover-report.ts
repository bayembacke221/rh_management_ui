/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TurnoverReportDto } from '../../models/turnover-report-dto';

export interface GetTurnoverReport$Params {

/**
 * Année pour le rapport
 */
  year: number;
}

export function getTurnoverReport(http: HttpClient, rootUrl: string, params: GetTurnoverReport$Params, context?: HttpContext): Observable<StrictHttpResponse<TurnoverReportDto>> {
  const rb = new RequestBuilder(rootUrl, getTurnoverReport.PATH, 'get');
  if (params) {
    rb.path('year', params.year, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TurnoverReportDto>;
    })
  );
}

getTurnoverReport.PATH = '/reports/turnover/{year}';
