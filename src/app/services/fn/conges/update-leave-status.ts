/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { LeaveApprovalDto } from '../../models/leave-approval-dto';
import { LeaveDto } from '../../models/leave-dto';

export interface UpdateLeaveStatus$Params {

/**
 * ID de la demande de congés
 */
  id: number;
      body: LeaveApprovalDto
}

export function updateLeaveStatus(http: HttpClient, rootUrl: string, params: UpdateLeaveStatus$Params, context?: HttpContext): Observable<StrictHttpResponse<LeaveDto>> {
  const rb = new RequestBuilder(rootUrl, updateLeaveStatus.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<LeaveDto>;
    })
  );
}

updateLeaveStatus.PATH = '/leaves/{id}/status';
