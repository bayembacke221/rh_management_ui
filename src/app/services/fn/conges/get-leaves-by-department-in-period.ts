/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { LeaveDto } from '../../models/leave-dto';

export interface GetLeavesByDepartmentInPeriod$Params {

/**
 * ID du département
 */
  departmentId: number;

/**
 * Date de début de la période
 */
  startDate: string;

/**
 * Date de fin de la période
 */
  endDate: string;
}

export function getLeavesByDepartmentInPeriod(http: HttpClient, rootUrl: string, params: GetLeavesByDepartmentInPeriod$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<LeaveDto>>> {
  const rb = new RequestBuilder(rootUrl, getLeavesByDepartmentInPeriod.PATH, 'get');
  if (params) {
    rb.path('departmentId', params.departmentId, {});
    rb.query('startDate', params.startDate, {});
    rb.query('endDate', params.endDate, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<LeaveDto>>;
    })
  );
}

getLeavesByDepartmentInPeriod.PATH = '/leaves/department/{departmentId}/period';
