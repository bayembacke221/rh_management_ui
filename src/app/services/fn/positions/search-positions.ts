/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Page } from '../../models/page';
import { Pageable } from '../../models/pageable';

export interface SearchPositions$Params {

/**
 * Mot-clé pour la recherche (titre ou description)
 */
  keyword?: string;

/**
 * Filtrer par statut actif/inactif
 */
  active?: boolean;

/**
 * Filtrer par département
 */
  departmentId?: number;

/**
 * Paramètres de pagination
 */
  pageable: Pageable;
}

export function searchPositions(http: HttpClient, rootUrl: string, params: SearchPositions$Params, context?: HttpContext): Observable<StrictHttpResponse<Page>> {
  const rb = new RequestBuilder(rootUrl, searchPositions.PATH, 'get');
  if (params) {
    rb.query('keyword', params.keyword, {});
    rb.query('active', params.active, {});
    rb.query('departmentId', params.departmentId, {});
    rb.query('pageable', params.pageable, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Page>;
    })
  );
}

searchPositions.PATH = '/api/positions/search';
