/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SalaryCostReportDto } from '../../models/salary-cost-report-dto';

export interface GetSalaryCostReport$Params {

/**
 * Année pour le rapport
 */
  year: number;
}

export function getSalaryCostReport(http: HttpClient, rootUrl: string, params: GetSalaryCostReport$Params, context?: HttpContext): Observable<StrictHttpResponse<SalaryCostReportDto>> {
  const rb = new RequestBuilder(rootUrl, getSalaryCostReport.PATH, 'get');
  if (params) {
    rb.path('year', params.year, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SalaryCostReportDto>;
    })
  );
}

getSalaryCostReport.PATH = '/reports/salary/{year}';
